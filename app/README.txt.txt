The source code runs through an emulator and therefore the Android SDK is needed for it to compile in Android Studio IDE. 
The target SDK described in the gradle file is 27 so when choosing an emulator, it should have an OS version 8.0 or higher.
The manifest file describes the Main Activity as the main run file.
The JDBC library is downloaded and added into libs directory.